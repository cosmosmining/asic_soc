# Pinned toolchain — single source of truth for CI and local runs.
# Sourced by scripts/CI; update versions ONLY here.

# --- Reproduced in this environment (sim/lint) ---
VERILATOR_VERSION=5.020
IVERILOG_VERSION=12.0
PYTHON_VERSION=3.11

# --- Introduced by later phases; pin confirmed on first real use ---
YOSYS_VERSION=0.46           # logic synthesis (riscv-soc, pd)        [planned]
OPENROAD_VERSION=2.0         # P&R                                    [planned]
OPENLANE_VERSION=2.x         # sky130 RTL->GDS flow                   [planned]
SBY_VERSION=0.40             # SymbiYosys formal (dv, fifo proofs)    [planned]
SKY130_PDK=sky130A           # via volare/ciel                        [planned]
SPIKE_COMMIT=TBD             # riscv-isa-sim for co-sim (dv)          [planned]
